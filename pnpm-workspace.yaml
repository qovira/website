# Qovira's own packages are exempt from the registry minimum-release-age
# cooldown — we consume them as soon as they publish (they're first-party).
minimumReleaseAgeExclude:
  - "@qovira/theme"
  - "@qovira/ui"
